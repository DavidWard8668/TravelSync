<?xml version="1.0" encoding="utf-8"?>
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies>
        <!-- Prevent app uninstallation -->
        <disable-keyguard />
        <expire-password />
        <force-lock />
        <limit-password />
        <reset-password />
        <wipe-data />
        <watch-login />
        
        <!-- Additional security policies for Second Chance -->
        <encrypted-storage />
        <disable-camera />
    </uses-policies>
    
    <!-- Device admin metadata -->
    <support-screen 
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />
        
    <!-- Admin description shown to user -->
    <description>
        Second Chance requires device administrator privileges to prevent uninstallation during recovery. 
        This ensures your recovery protection remains active and cannot be easily bypassed during moments of weakness.
        
        Permissions needed:
        • Prevent app uninstallation without PIN
        • Monitor device security settings
        • Maintain protection after device restart
        
        Your recovery admin will be notified if these protections are disabled.
    </description>
</device-admin>